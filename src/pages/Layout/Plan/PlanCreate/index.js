/**
 * 创建分析计划页面。
 */
import React from 'react';

const PlanCreate = () => (
    <div style={{
        margin: 8,
        padding: 8,
        backgroundColor: '#fff',
        borderRadius: 16,
    }}>
    </div>
);

export default PlanCreate();